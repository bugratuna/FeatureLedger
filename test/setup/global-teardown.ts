/**
 * Global teardown for E2E tests.
 * Runs once after all test suites complete.
 */
export default async function globalTeardown() {
  // Placeholder — cleanup shared resources if needed (e.g., close DB pools)
}
